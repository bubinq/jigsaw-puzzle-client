## Frontend environment variables (Vite)
#
# If set, the frontend will call the Go API directly (cross-origin in dev).
# Example: http://localhost:8080
# If unset, the frontend uses the dev-server proxy at /__api (recommended).
VITE_API_BASE_URL=

